---
phase: 05-site-foundation-and-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/astro.config.mjs
  - web/src/content.config.ts
  - web/src/content/docs/index.md
  - .github/workflows/deploy.yml
autonomous: true
requirements: [DLVR-02, DLVR-03, DLVR-04, DLVR-05, DEPL-01, DEPL-02]

must_haves:
  truths:
    - "Astro dev server starts and serves a landing page at localhost"
    - "Starlight sidebar, search, dark mode, and responsive layout are functional"
    - "GitHub Actions workflow exists and will deploy to GitHub Pages on push"
  artifacts:
    - path: "web/package.json"
      provides: "Astro 6 + Starlight 0.38 project dependencies"
      contains: "@astrojs/starlight"
    - path: "web/astro.config.mjs"
      provides: "Site config with GitHub Pages base path and sidebar structure"
      contains: "base: '/matlab-course'"
    - path: "web/src/content.config.ts"
      provides: "Content collection with docsLoader"
      contains: "docsLoader"
    - path: "web/src/content/docs/index.md"
      provides: "Course landing page with hero and navigation"
      contains: "template: splash"
    - path: ".github/workflows/deploy.yml"
      provides: "GitHub Actions deploy pipeline"
      contains: "withastro/action"
  key_links:
    - from: "web/astro.config.mjs"
      to: "web/src/content/docs/"
      via: "Starlight sidebar config references content slugs"
      pattern: "slug.*01-first-steps"
    - from: ".github/workflows/deploy.yml"
      to: "web/"
      via: "withastro/action path parameter"
      pattern: "path.*web"
---

<objective>
Scaffold the Astro 6 + Starlight 0.38 project in web/, configure for GitHub Pages deployment, create the landing page, and set up the GitHub Actions deploy workflow.

Purpose: Establish the site foundation that all content migration builds on, and enable continuous deployment from the start.
Output: Working Astro dev server with landing page, sidebar structure for 8 modules + reference, and deploy pipeline ready for GitHub Pages.
</objective>

<execution_context>
@/Users/stig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-site-foundation-and-deploy/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro/Starlight project and configure for GitHub Pages</name>
  <files>web/package.json, web/astro.config.mjs, web/src/content.config.ts, web/tsconfig.json</files>
  <action>
1. Create the Astro project with Starlight template:
   ```bash
   npm create astro@latest -- --template starlight ./web --no-install
   ```
   Then cd into web/ and run `npm install`.

2. Verify installed versions: Astro 6.x and @astrojs/starlight 0.38.x. If the template installs older versions, update package.json to `"astro": "^6.0.4"` and `"@astrojs/starlight": "^0.38.1"` and re-run `npm install`.

3. Replace web/astro.config.mjs with:
   - site: 'https://stigsb.github.io'
   - base: '/matlab-course'
   - Starlight integration with title "MATLAB for Computational Neuroscience"
   - Sidebar with "Course Modules" group containing all 8 modules (labeled "Module 1: First Steps" through "Module 8: Attractor Networks") with explicit slugs (01-first-steps, 02-vectors-and-plotting, etc.)
   - Sidebar with "Reference" group using autogenerate: { directory: 'reference' }

4. Replace web/src/content.config.ts with the Starlight 0.38 content layer config using docsLoader() and docsSchema() from @astrojs/starlight/loaders and @astrojs/starlight/schema.

5. Delete any template example content in web/src/content/docs/ (Starlight template ships with example pages).

6. Verify with `cd web && npm run build` -- build should succeed (may warn about missing content, that's fine).

Do NOT install Preact, nanostores, or any Phase 6 dependencies.
  </action>
  <verify>
    <automated>cd /Users/stig/git/stigsb/matlab-course/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>web/ contains a working Astro 6 + Starlight 0.38 project that builds successfully with GitHub Pages base path configured and sidebar structure for all 8 modules + reference.</done>
</task>

<task type="auto">
  <name>Task 2: Create landing page and GitHub Actions deploy workflow</name>
  <files>web/src/content/docs/index.md, .github/workflows/deploy.yml</files>
  <action>
1. Create web/src/content/docs/index.md as the course landing page:
   ```markdown
   ---
   title: MATLAB for Computational Neuroscience
   description: A visual-first course teaching MATLAB and math foundations for NEVR3004
   template: splash
   hero:
     tagline: From zero to neural network models -- one visual concept at a time
     actions:
       - text: Start Module 1
         link: /matlab-course/01-first-steps/
         icon: right-arrow
         variant: primary
       - text: Browse all modules
         link: /matlab-course/02-vectors-and-plotting/
         variant: minimal
   ---
   ```

2. Create .github/workflows/deploy.yml at the REPOSITORY ROOT (not inside web/):
   ```yaml
   name: Deploy to GitHub Pages

   on:
     push:
       branches: [main]
     workflow_dispatch:

   permissions:
     contents: read
     pages: write
     id-token: write

   jobs:
     build:
       runs-on: ubuntu-latest
       steps:
         - name: Checkout
           uses: actions/checkout@v5
         - name: Build Astro site
           uses: withastro/action@v5
           with:
             path: ./web
             node-version: 22

     deploy:
       needs: build
       runs-on: ubuntu-latest
       environment:
         name: github-pages
         url: ${{ steps.deployment.outputs.page_url }}
       steps:
         - name: Deploy to GitHub Pages
           id: deployment
           uses: actions/deploy-pages@v4
   ```

3. Verify build still succeeds with landing page content.
  </action>
  <verify>
    <automated>cd /Users/stig/git/stigsb/matlab-course/web && npm run build 2>&1 | tail -5 && test -f /Users/stig/git/stigsb/matlab-course/.github/workflows/deploy.yml && echo "deploy.yml exists"</automated>
  </verify>
  <done>Landing page renders at site root with hero section and navigation links. GitHub Actions workflow file exists at repo root and references web/ subdirectory with Node 22.</done>
</task>

</tasks>

<verification>
- `cd web && npm run build` succeeds without errors
- `cd web && npm run preview` serves the landing page with Starlight chrome (sidebar, search icon, dark mode toggle)
- `.github/workflows/deploy.yml` exists at repo root with correct path and node-version
- Sidebar shows 8 module entries + Reference group (modules will 404 until Plan 02 migrates content)
</verification>

<success_criteria>
- Astro 6.x + Starlight 0.38.x installed and building
- Landing page with hero section accessible at dev server root
- Sidebar configured with all 8 modules and reference section
- GitHub Actions deploy workflow ready (will activate on first push after GitHub Pages is enabled)
- Dark mode toggle, search, and responsive layout functional (Starlight built-ins)
</success_criteria>

<output>
After completion, create `.planning/phases/05-site-foundation-and-deploy/05-01-SUMMARY.md`
</output>
